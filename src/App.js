import CardContainer from "./components/CardContainer/CardContainer";

function App() {
  return (
    <div>
      <CardContainer />
      {/* <Mouse
        render={({ x, y }) => (
          <p>
            The mouse position is ({x}, {y})
          </p>
        )}
      /> */}
    </div>
  );
}

export default App;
